name: wonder_api
description: "A project for accessing the wonder wix api."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_json_view: ^1.1.5
  flutter_speed_dial: ^7.0.0
  font_awesome_flutter: ^10.8.0
  recase: ^4.1.0
  wonder:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
