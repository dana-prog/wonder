// import 'package:test/test.dart';
// import 'package:wonder/src/constants.dart';
// import 'package:wonder/src/wix/sdk/wix_client.dart';
//
// void main() {
//   group('WixClient', () {
//     test('should log in and get member tokens', () async {
//       final wixClient = WixClient(
//         clientId: clientId,
//       );
//
//       await wixClient.auth.login(email, password);
//
//       final tokens = wixClient.auth.tokens;
//       expect(tokens, isNotNull);
//       expect(tokens!.accessToken, isNotEmpty);
//       expect(tokens.refreshToken, isNotEmpty);
//     });
//   });
// }
